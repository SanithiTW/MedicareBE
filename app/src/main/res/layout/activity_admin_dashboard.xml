<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- Header -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:background="#FFF3F3"
        android:elevation="4dp"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="24dp">

        <ImageView
            android:id="@+id/adminIcon"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:contentDescription="Admin Icon"
            android:padding="18dp"
            android:src="@drawable/ic_admin"
            android:background="@drawable/circle_light_red" />

        <TextView
            android:id="@+id/adminWelcomeText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:text="Welcome, Super Admin!"
            android:textColor="#D50000"
            android:textSize="22sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- Create Admin Card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="60dp"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Card Container with Rounded Background -->
        <LinearLayout
            android:id="@+id/createAdminCard"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:elevation="6dp"
            android:padding="20dp"
            android:gravity="center"
            android:layout_margin="12dp"
            android:background="#78F88DEF">

            <!-- Icon with Red Circular Background -->
            <ImageView
                android:id="@+id/CreateAdminIcon"
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:padding="18dp"
                android:src="@drawable/ic_create_admin"
                android:contentDescription="Create Admin Icon"
                android:background="@drawable/circle_light_red"/>

            <!-- Text Box with Inline Rounded Background -->
            <TextView
                android:id="@+id/createAdminText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:gravity="center"
                android:text="Create Admin"
                android:textColor="#FFFFFF"
                android:textSize="18sp"
                android:textStyle="bold"
                android:padding="12dp"
                android:background="#F8BB4BCE" />
        </LinearLayout>
    </LinearLayout>

</LinearLayout>
